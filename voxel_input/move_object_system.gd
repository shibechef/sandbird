extends Node
class_name MoveObjectSystem

## so probably just while holding g see how much the mouse has moved in 2d space
## see the dir of the camera, use that to get an x y and z of the movement
## if 1 axis is clicked during this, it will only take the comp of that axis
## support clicking 2 axes + unselecting an axis
## probably gonna use this for non grid aligned so make sure thats an option

func move_object() -> void:
	return
